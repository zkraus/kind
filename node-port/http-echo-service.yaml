apiVersion: v1
kind: Service
metadata:
  name: http-echo
spec:
  type: NodePort
  selector:
    app: http-echo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30001
