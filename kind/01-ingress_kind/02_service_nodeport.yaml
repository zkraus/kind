kind: Service
apiVersion: v1
metadata:
  name: echo-service
spec:
  type: NodePort
  selector:
    app: echo-app
  ports:
  - port: 80 ## for internal cluster network routing
    targetPort: 8080 ## targeted pods ports
    nodePort: 30080 ## only for NodePort, directly connecting to kubernetes node port